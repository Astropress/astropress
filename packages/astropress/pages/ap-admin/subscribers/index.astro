---
// Subscriber management has moved into Settings > Newsletter tab.
// Auth is resolved by middleware; we read adminUser to satisfy ZTA invariant.
const _adminUser = Astro.locals.adminUser;
return Astro.redirect("/ap-admin/settings?tab=newsletter", 301);
---
