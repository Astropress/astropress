// Route entrypoint for POST /ap/contact — the handler lives in src so it is
// unit-tested and mutation-scored alongside the rest of the runtime.
export { POST } from "../../src/contact-endpoint.js";
