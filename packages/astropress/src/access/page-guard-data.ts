// stryker-disable-file: data-only — default redirect-path strings; behaviour is asserted via integration/e2e flows, not unit assertions on the literal.

export const DEFAULT_FORBIDDEN_PATH = "/ap-admin?error=insufficient-permissions";
export const DEFAULT_LOGIN_PATH = "/ap-admin/login";
