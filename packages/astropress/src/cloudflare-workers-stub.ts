// stryker-disable-file: data-only — single placeholder export; no logic to mutate.
// audit-boundary: opaque-passthrough -- module-boundary value; narrowed at consumer
export const env = {} as Record<string, unknown>;
