[package]
name = "astropress-cli"
version = "0.0.1"
edition = "2021"
description = "Astropress CLI for project scaffolding, WordPress import, sync, and deployment."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
