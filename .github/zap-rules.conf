# ZAP baseline rule configuration for astropress
# Rule format: <ruleId>  <action>  <optionalReason>
#
# 10055 — CSP: style-src unsafe-inline
#   Astro's static build inlines component styles. unsafe-inline is intentional
#   for the github-pages example (no nonce infrastructure). Acknowledged.
10055	IGNORE	(CSP: style-src unsafe-inline — intentional for static Astro build)
