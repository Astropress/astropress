# Security tool suite false-positive suppressions for astropress
# Format: <tool>:<id_or_check>  <reason>
#
# nikto
# OSVDB-3233: /icons/ — Apache default icons dir; not present in this static build but
#   nikto flags any server that doesn't return 404 for this path. False positive.
# OSVDB-3268: /icons/README — same as above.
nikto:OSVDB-3233  Static Astro build — no Apache icons directory
nikto:OSVDB-3268  Static Astro build — no Apache icons README

# whatweb
# X-Powered-By absent is expected — server-side headers are intentionally stripped by
#   serve-static-with-security-headers.mjs. Not a leak.
